import { ComingSoon } from "@/components/ui/coming-soon";

export default function CalendarPage() {
  return (
    <ComingSoon
      eyebrow="a month at a glance"
      title="Calendar"
      note="Every day with a memory, a rating, or a letter will glow softly here — Phase 4."
    />
  );
}
